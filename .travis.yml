language: node_js
node_js:
  - "node"
cache:
  yarn: true
script:
  - "yarn lint"
  - "yarn test"
  - "env NODE_ENV=production yarn build"
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build/dist
  on:
    branch: master
